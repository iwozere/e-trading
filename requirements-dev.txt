-r requirements.txt
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
# mypy pinned exactly: the mypy=0 gate (with the pandas/anthropic/redis
# follow_imports=skip config in pyproject.toml) is verified against this build.
# A newer mypy tightens inference and reopens the count; bump deliberately.
mypy==1.16.1
autoflake>=2.2.0
# Pyright pinned exactly: the type-check gate (scripts/typecheck.py + CI) must run
# a reproducible pyright across machines. The `pyright` PyPI package (pyright-python)
# bundles this exact node pyright build, so `python -m pyright` == CI == local.
# Do not rely on `npx pyright`, which floats to latest and drifts the error count.
pyright==1.1.411
# Type stubs for mypy
types-croniter
types-openpyxl
types-psutil
types-reportlab
types-requests
types-tqdm
# pandas-stubs is what pyright needs to reach zero errors. mypy tolerates it
# via the follow_imports=skip override in pyproject.toml, so both checkers now
# run cleanly in this one environment (no split-brain).
pandas-stubs
# Optional runtime deps imported by active code (redis/zstandard caching, san
# data, onnx model export, slowapi rate limiting). Present so the checkers
# resolve the imports in the type-check environment, which installs only this
# file (not requirements-webui.txt / requirements-full.txt). bcrypt is a real
# runtime dep and lives in requirements.txt (pulled in via -r above).
redis
zstandard
san
onnxruntime
onnxconverter-common
onnxmltools
slowapi