{
    "name": "RSIOrBBExitMixin",
    "params": {
        "x_rsi_period": {
            "type": "int",
            "low": 10,
            "high": 20,
            "default": 14
        },
        "x_rsi_overbought": {
            "type": "float",
            "low": 65,
            "high": 80,
            "default": 70
        },
        "x_bb_period": {
            "type": "int",
            "low": 14,
            "high": 22,
            "default": 20
        },
        "x_bb_dev": {
            "type": "float",
            "low": 1.8,
            "high": 2.2,
            "default": 2.0
        }
    },
    "description": "Exit strategy based on either RSI or Bollinger Bands cross-over conditions. Exits when RSI crosses overbought line (previous above, current below) OR price crosses BB top line (previous above, current below)."
}
