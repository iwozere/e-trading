{
    "name": "EOMBreakdownExitMixin",
    "trading_style": "swing_trading",
    "description": "Breakdown + EOM negative for swing exits (Generic Fallback)",
    "params": {
        "x_breakdown_threshold": {
            "type": "float",
            "low": 0.002,
            "high": 0.008,
            "default": 0.003,
            "description": "Breakdown threshold below support"
        }
    },
    "indicator_params": {
        "eom_timeperiod": {
            "type": "int",
            "low": 12,
            "high": 20,
            "default": 14,
            "description": "EOM smoothing period"
        },
        "sr_lookback_bars": {
            "type": "int",
            "low": 2,
            "high": 5,
            "default": 3,
            "description": "S/R swing detection lookback"
        },
        "volume_sma_period": {
            "type": "int",
            "low": 15,
            "high": 30,
            "default": 20,
            "description": "Volume SMA period"
        },
        "atr_period": {
            "type": "int",
            "low": 12,
            "high": 20,
            "default": 14,
            "description": "ATR period for volatility confirmation"
        }
    }
}