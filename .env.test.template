# ==========================================
# TEST DATABASE CONFIGURATION (PostgreSQL)
# ==========================================
# ⚠️ CRITICAL: This file configures the TEST database for unit tests
# NEVER point this to your production database!
#
# To use:
# 1. Copy this file to config/donotshare/.env.test
# 2. Update the values below with your test database credentials
# 3. Set TEST_DB_URL environment variable before running tests:
#    PowerShell: $env:TEST_DB_URL = "postgresql+psycopg2://test_user:test_pass@localhost/e_trading_test"
#    Bash:       export TEST_DB_URL="postgresql+psycopg2://test_user:test_pass@localhost/e_trading_test"

# Test Database URL - MUST be different from production!
# MUST be PostgreSQL (no SQLite support in this project)
TEST_DB_URL=postgresql+psycopg2://test_user:test_password@localhost:5432/e_trading_test

# PostgreSQL Admin Connection (for creating/dropping test databases)
# This should point to the 'postgres' database with admin privileges
PG_ADMIN_URL=postgresql+psycopg2://postgres:admin_password@localhost:5432/postgres

# Individual PostgreSQL credentials (alternative to TEST_DB_URL)
# The test conftest will construct TEST_DB_URL from these if not set directly
POSTGRES_TEST_USER=test_user
POSTGRES_TEST_PASSWORD=test_password
POSTGRES_TEST_HOST=localhost
POSTGRES_TEST_PORT=5432
POSTGRES_TEST_DB=e_trading_test

# Alembic test database URL (used by migration tests)
ALEMBIC_DB_URL=postgresql+psycopg2://test_user:test_password@localhost:5432/e_trading_test
